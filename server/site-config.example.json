{
  "_comment": "复制为 site-config.json 后填入你自己的值（该文件已被 .gitignore 排除）。也可以用同名大写环境变量覆盖：ADMIN_WECHAT / ADMIN_PASSWORD / JWT_SECRET。都不填时，JWT 密钥与管理员口令会在首次启动时随机生成并写入数据目录。",
  "adminWechat": "你的微信号或手机号（会展示在注册/找回密码/认证页面，供同学联系你）",
  "adminPassword": "后台管理员口令"
}
